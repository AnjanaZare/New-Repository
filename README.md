# New-Repository

# MySQL-backed Java App

This project is a Java application that connects to a MySQL database.
- **Tech stack:** Java, JDBC/Spring Data (depending on project), MySQL
- **Setup:**
  1. Configure database credentials in application properties.
  2. Run migrations or create schema if required.
  3. Build and run the app.
- **Commands:**
  - Maven: `./mvnw spring-boot:run`
  - Gradle: `./gradlew bootRun`
